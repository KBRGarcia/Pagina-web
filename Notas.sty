-------------------------------------------Comandos de Docker-------------------------------------------

docker pull <image_name>:
  Descarga una imagen de Docker desde un repositorio público o privado.

docker run <image_name>:
  Ejecuta un contenedor basado en una imagen específica.

docker ps:
  Muestra los contenedores en ejecución.

docker ps -a:
  Muestra todos los contenedores, incluso los detenidos.

docker stop <container_id or container_name>:
  Detiene un contenedor en ejecución.

docker start <container_id or container_name>:
  Inicia un contenedor detenido.

docker rm <container_id or container_name>:
  Elimina un contenedor.

docker images:
  Lista todas las imágenes de Docker disponibles localmente.

docker rmi <image_name or image_id>:
  Elimina una imagen de Docker.

docker build <path_to_Dockerfile>:
  Construye una imagen de Docker a partir de un Dockerfile.

docker-compose up:
  Construye y levanta servicios definidos en un archivo docker-compose.yml.

docker-compose down:
  Detiene y elimina los servicios definidos en un archivo docker-compose.yml.

docker exec -it <container_id or container_name> <command>:
  Ejecuta un comando dentro de un contenedor en ejecución.

docker inspect <container_id or container_name>:
  Muestra información detallada sobre un contenedor.

docker logs <container_id or container_name>:
  Muestra los registros de salida de un contenedor.

docker network ls:
  Lista las redes de Docker disponibles en el sistema.

docker network create <network_name>:
  Crea una nueva red de Docker.

docker network connect <network_name> <container_id or container_name>:
  Conecta un contenedor a una red específica.

docker volume ls:
  Lista los volúmenes de Docker disponibles en el sistema.

docker volume create <volume_name>:
  Crea un nuevo volumen de Docker.

docker volume rm <volume_name>:
  Elimina un volumen de Docker.

docker cp <local_file> <container_id>:
  ntainer_path>:

  Copia archivos entre el sistema local y un contenedor.

docker-compose build:
  Construye servicios definidos en un archivo docker-compose.yml.

docker-compose up -d:
  Construye y levanta servicios en segundo plano.

docker-compose stop:
  Detiene servicios definidos en un archivo docker-comp

docker-compose restart:
  Reinicia servicios definidos en un archivo docker-compose.yml.

docker-compose exec <service_name> <command>:
  Ejecuta un comando dentro de un servicio en ejecución.

docker-compose logs <service_name>:
  Muestra los registros de salida de un servicio.

docker-compose down -v:
  Detiene y elimina servicios y volúmenes definidos en un archivo docker-compose.yml.

docker stats:
  Muestra el uso de recursos de los contenedores en ejecución.

docker system prune:
  Limpia recursos no utilizados, como contenedores detenidos y redes no utilizadas.

docker swarm init:
  Inicializa un clúster de Docker Swarm en el nodo actual.

docker swarm join:
  Une un nodo al clúster de Docker Swarm.

docker service ls:
  Lista los servicios en ejecución en un clúster de Docker Swarm.

docker service create:
  Crea un nuevo servicio en un clúster de Docker Swarm.

docker service scale:
  Escala el número de réplicas de un servicio en un clúster de Docker Swarm.

docker service update:
  Actualiza la configuración de un servicio en un clúster de Docker Swarm.

docker stack deploy:
  Despliega una aplicación definida en un archivo docker-compose.yml en un clúster de Docker Swarm.

docker node ls:
  Lista los nodos en un clúster de Docker Swarm.

docker node inspect:
  Muestra información detallada sobre un nodo en un clúster de Docker Swarm.

docker login:
  Inicia sesión en un registro de Docker.

docker logout:
  Cierra sesión en un registro de Docker.

docker tag <source_image>:<tag> <target_image>:<tag>:
  Etiqueta una imagen de Docker con un nuevo nombre y/o etiqueta.

docker push <image_name>:
  Sube una imagen de Docker a un registro de Docker.

docker pull <registry_url>/<image_name>:<tag>:
  Descarga una imagen de Docker desde un registro específico.

docker build -t <image_name>:<tag> <path_to_Dockerfile>:
  Construye una imagen de Docker y le asigna un nombre y etiqueta.

docker run -d <image_name>:
  Ejecuta un contenedor en segundo plano.

docker run -it <image_name> <command>:
  Ejecuta un comando interactivo dentro de un contenedor.

docker run -p <host_port>:<container_port> <image_name>:
  Mapea un puerto del host al puerto del contenedor.

docker run --name <container_name> <image_name>:
  Asigna un nombre específico a un contenedor al iniciarlo.

docker inspect <image_name or image_id>:
  Muestra información detallada sobre una imagen de Docker.

docker history <image_name>:
  Muestra el historial de capas de una imagen de Docker.

docker export <container_id> > <file.tar>:
  Exporta el sistema de archivos de un contenedor como un archivo TAR.

docker import <file.tar>:
  Importa un sistema de archivos exportado previamente como una imagen de Docker.

docker commit <container_id> <new_image_name>:
  Crea una nueva imagen a partir de un contenedor en ejecución.

docker exec <container_id or container_name> <command>:
  Ejecuta un comando dentro de un contenedor en ejecución.

docker pause <container_id or container_name>:
  Pausa la ejecución de un contenedor.

docker unpause <container_id or container_name>:
  Reanuda la ejecución de un contenedor pausado.

docker stats <container_id or container_name>:
  Muestra el uso de recursos en tiempo real de un contenedor.

docker top <container_id or container_name>:
  Muestra los procesos en ejecución dentro de un contenedor.

docker events:
  Muestra eventos de Docker en tiempo real.

docker system df:
  Muestra el espacio utilizado y disponible en el sistema Docker.

docker system prune -a:
  Limpia todos los recursos no utilizados, incluidos los volúmenes.

docker search <term>:
  Busca imágenes de Docker en un registro.

docker port <container_id or container_name>:
  Muestra los puertos mapeados de un contenedor.

docker wait <container_id or container_name>:
  Espera a que un contenedor finalice y muestra su código de salida.

docker version:
  Muestra información sobre la versión de Docker instalada.

docker network inspect <network_name>:
  Muestra detalles sobre una red de Docker.

docker network disconnect <network_name> <container_id or container_name>:
  Desconecta un contenedor de una red.

docker volume inspect <volume_name>:
  Muestra información detallada sobre un volumen de Docker.

docker volume prune:
  Elimina todos los volúmenes no utilizados.

docker diff <container_id or container_name>:
  Muestra los cambios en el sistema de archivos de un contenedor en comparación con su imagen base.

docker save <image_name>:<tag> -o <file.tar>:
  Guarda una imagen de Docker en un archivo TAR.

docker load -i <file.tar>:
  Carga una imagen de Docker desde un archivo TAR.

docker exec -u <user> <container_id or container_name> <command>:
  Ejecuta un comando como un usuario específico dentro de un contenedor.

docker inspect --format='{{.State.Status}}' <container_id or container_name>:
  Obtiene el estado de ejecución de un contenedor.

docker ps -q:
  Muestra solo los IDs de los contenedores en ejecución.

docker image prune:
  Elimina las imágenes no utilizadas.

docker volume create --name <volume_name>:
  Crea un volumen de Docker con un nombre específico.

docker-compose logs -f:
  Muestra los registros en tiempo real de los servicios definidos en un archivo docker-compose.yml.







  

-------------------------------------------Comandos de WIndows-------------------------------------------

dir:
  Muestra una lista de archivos y subdirectorios en el directorio actual.
  
cd:
  Cambia el directorio actual.
  
cls:
  Limpia la pantalla del símbolo del sistema.
  
copy:
  Copia uno o más archivos de un lugar a otro.
  
del:
  Elimina uno o más archivos.
  
move:
  Mueve archivos de un lugar a otro.
  
ren:
  Cambia el nombre de un archivo o directorio.
  
mkdir:
  Crea un nuevo directorio.
  
rmdir:
  Elimina un directorio.
  
type:
  Muestra el contenido de un archivo de texto.
  
echo:
  Muestra mensajes o activa/desactiva el eco de comandos.
  
ping:
  Envía paquetes de datos a una dirección IP para probar la conectividad de red.
  
ipconfig:
  Muestra la configuración de red, incluyendo la dirección IP, la puerta de enlace y la máscara de subred.
  
netstat:
  Muestra las conexiones de red, tablas de enrutamiento y estadísticas de interfaz.
  
tasklist:
  Muestra una lista de los procesos en ejecución.
  
taskkill:
  Termina uno o más procesos.
  
shutdown:
  Apaga o reinicia el sistema.
  
systeminfo:
  Muestra información sobre el sistema, incluyendo la versión de Windows y la configuración de hardware.
  
sfc /scannow:
  Escanea y repara archivos de sistema corruptos.
  
chkdsk:
  Comprueba y repara errores en el disco duro.
  
format:
  Formatea un disco o partición.
  
xcopy:
  Copia archivos y directorios con más opciones que el comando "copy".
  
find:
  Busca una cadena de texto en archivos.
  
attrib:
  Muestra o cambia los atributos de archivo.
  
diskpart:
  Gestiona discos, particiones y volúmenes.
  
net:
  Administra recursos de red (usuarios, grupos, impresoras, etc.).
  
subst:
  Asigna una letra de unidad a una ruta de acceso.
  
assoc:
  Muestra o modifica las asociaciones de extensiones de archivo.
  
tree:
  Muestra una estructura de directorios como un árbol.
  
powercfg:
  Configura la administración de energía.
  
cipher:
  Muestra o cambia el cifrado de archivos y directorios en unidades NTFS.
  
taskmgr:
  Abre el Administrador de tareas de Windows.
  
msconfig:
  Abre la Utilidad de Configuración del Sistema de Windows.
  
notepad:
  Abre el Bloc de notas.
  
regedit:
  Abre el Editor del Registro de Windows.
  
shutdown:
  Apaga o reinicia el sistema.
  
calc:
  Abre la calculadora de Windows.
  
hostname:
  Muestra el nombre de host del equipo.
  
netsh:
  Configura parámetros de red.
  
tracert:
  Rastrea la ruta que toman los paquetes de datos hasta su destino.
  
assoc:
  Muestra o modifica las asociaciones de extensiones de archivo.
  
chkntfs:
  Muestra o modifica el chequeo de disco al inicio del sistema.
  
color:
  Cambia los colores del fondo y del texto en la ventana del símbolo del sistema.
  
compact:
  Comprime o descomprime archivos en NTFS.
  
convert:
  Convierte volúmenes FAT a NTFS.
  
date:
  Muestra o establece la fecha del sistema.
  
defrag:
  Desfragmenta el disco duro.
  
driverquery:
  Muestra una lista de los controladores instalados en el sistema.
  
hostname:
  Muestra el nombre de host del equipo.
  
logoff:
  Cierra la sesión actual del usuario.
  
mode:
  Configura dispositivos de sistema.
  
netstat:
  Muestra las conexiones de red, tablas de enrutamiento y estadísticas de interfaz.
  
path:
  Muestra o establece la variable de entorno de ruta de acceso.
  
pause:
  Detiene temporalmente la ejecución de un script por un período de tiempo.
  
print:
  Imprime un archivo de texto en una impresora.
  
prompt:
  Personaliza el texto del indicador del símbolo del sistema.
  
quser:
  Muestra la información de los usuarios que están conectados al sistema.
  
reg:
  Realiza operaciones en el Registro de Windows.
  
replace:
  Reemplaza archivos.
  
rmdir /s:
  Elimina un directorio y su contenido de forma recursiva.
  
set:
  Muestra, establece o elimina variables de entorno.
  
setx:
  Establece variables de entorno persistentes.
  
shutdown:
  Apaga o reinicia el sistema.
  
sort:
  Ordena la salida de un archivo de texto.
  
subst:
  Asigna una letra de unidad a una ruta de acceso.
  
systeminfo:
  Muestra información sobre el sistema, incluyendo la versión de Windows y la configuración de hardware.
  
time:
  Muestra o establece la hora del sistema.
  
timeout:
  Espera un período de tiempo antes de continuar con la ejecución de un script.
  
title:
  Establece el título de la ventana del símbolo del sistema.
  
tracert:
  Rastrea la ruta que toman los paquetes de datos hasta su destino.
  
type:
  Muestra el contenido de un archivo de texto.
  
ver:
  Muestra la versión de Windows.
  
vol:
  Muestra el nombre y el número de serie del volumen del disco.
  
whoami:
  Muestra información sobre el usuario y el grupo de seguridad actuales.
  
wmic:
  Ejecuta comandos específicos de Instrumental de Administración de Windows (WMI).
  
xcopy:
  Copia archivos y directorios con más opciones que el comando "copy".
  
break:
  Establece o elimina el control de interrupción en un script por lotes.
  
deltree:
  Elimina un directorio y su contenido de forma recursiva (similar a rmdir /s).
  
expand:
  Expande uno o más archivos comprimidos.
  
help:
  Proporciona información sobre los comandos de Windows.







  

  -------------------------------------------Comandos de GIT-------------------------------------------  

  git init:
  Inicializa un nuevo repositorio Git en un directorio.
  
git clone [URL]:
  Clona un repositorio Git existente desde una URL remota hacia tu sistema local.
  
git add [archivos]:
  Añade archivos al área de preparación (staging) para ser incluidos en el próximo commit.
  
git commit -m "[mensaje]":
  Realiza un commit con los archivos previamente añadidos al área de preparación, incluyendo un mensaje descriptivo.
  
git status:
  Muestra el estado actual del repositorio, incluyendo archivos modificados, añadidos o eliminados, así como archivos en el área de preparación.
  
git log:
  Muestra un historial de commits, incluyendo el autor, la fecha y el mensaje de cada commit.
  
git branch:
  Lista todas las ramas en el repositorio. Agregando un nombre de rama después del comando, puedes crear una nueva rama.
  
git checkout [rama]:
  Cambia a la rama especificada.
  
git merge [rama]:
  Fusiona los cambios de una rama con la rama actual.
  
git pull:
  Descarga los cambios desde el repositorio remoto y los fusiona con tu rama actual.
  
git push:
  Sube los commits locales al repositorio remoto.
  
git remote -v:
  Muestra las URL de los repositorios remotos asociados con tu repositorio local.
  
git diff:
  Muestra las diferencias entre los archivos en el directorio de trabajo y los archivos en el área de preparación.
  
git reset [archivo]:
  Quita un archivo del área de preparación, pero conserva los cambios locales.
  
git reset --hard:
  Restablece el estado del directorio de trabajo y del área de preparación a la última confirmación.
  
git tag [nombre]:
  Crea un nuevo tag para marcar un punto específico en la historia del repositorio.
  
git fetch:
  Descarga los cambios desde el repositorio remoto, pero no los fusiona con tu rama actual.
  
git remote add [nombre] [URL]:
  Agrega un nuevo repositorio remoto con un nombre específico.
  
git remote rm [nombre]:
  Elimina un repositorio remoto asociado.
  
git rm [archivo]:
  Elimina un archivo del repositorio y del sistema de archivos.
  
git mv [archivo_original] [archivo_nuevo]:
  Cambia el nombre de un archivo en el repositorio.
  
git show [objeto]:
  Muestra información sobre cualquier objeto Git, como un commit, un tag o un commit específico.
  
git stash:
  Guarda temporalmente los cambios locales en una pila de cambios (stash) para que puedas trabajar en otra cosa.
  
git stash apply:
  Aplica el cambio más reciente guardado en la pila de cambios al directorio de trabajo.
  
git stash list:
  Lista todos los cambios guardados en la pila de cambios.
  
git stash drop:
  Elimina el cambio más reciente guardado en la pila de cambios.
  
git cherry-pick [commit]:
  Aplica los cambios de un commit específico a la rama actual.
  
git rebase [rama]:
  Reorganiza la historia del commit para que la rama actual se derive de la rama especificada.
  
git bisect:
  Ayuda a encontrar el commit que introdujo un bug mediante la búsqueda binaria.
  
git grep [palabra_clave]:
  Busca ocurrencias de una palabra clave en los archivos del repositorio.
  
git blame [archivo]:
  Muestra quién modificó por última vez cada línea de un archivo y en qué commit.
  
git config:
  Permite configurar opciones de Git, como el nombre de usuario, el correo electrónico y alias de comandos.
  
git log --graph --oneline --decorate:
  Muestra un historial de commits de manera gráfica y resumida.
  
git show-branch:
  Muestra las ramas y sus últimos commits.
  
git remote update:
  Actualiza la información de los repositorios remotos.
  
git archive [rama] --format=zip --output=[archivo.zip]:
  Crea un archivo ZIP de una rama específica.
  
git submodule add [URL]:
  Agrega un submódulo al repositorio.
  
git submodule update --init --recursive:
  Clona y actualiza todos los submódulos del repositorio.
  
git clean -n:
  Muestra los archivos no rastreados que serán eliminados del directorio de trabajo.
  
git bisect:
  Ayuda a encontrar un commit específico que introdujo un problema al usar una búsqueda binaria.
  
git config --global user.name "[nombre]":
  Establece el nombre de usuario global para los commits.
  
git config --global user.email "[email]":
  Establece el correo electrónico global para los commits.
  
git config --global alias.[alias] "[comando]":
  Crea un alias para un comando Git específico.
  
git fetch [repositorio] [rama]:
  Descarga los cambios de una rama específica de un repositorio remoto.
  
git branch -d [rama]:
  Elimina una rama local después de haber fusionado sus cambios.
  
git branch -D [rama]:
  Forzosamente elimina una rama local, incluso si no se han fusionado los cambios.
  
git checkout -b [nueva_rama]:
  Crea y cambia a una nueva rama en una sola operación.
  
git diff [rama1] [rama2]:
  Muestra las diferencias entre dos ramas.
  
git diff --staged:
  Muestra las diferencias entre los archivos en el área de preparación y el último commit.
  
git log --author="[autor]":
  Filtra el historial de commits por autor.
  
git log --since="[fecha]":
  Muestra los commits realizados desde una fecha específica.
  
git log --until="[fecha]":
  Muestra los commits realizados hasta una fecha específica.
  
git log --grep="[palabra_clave]":
  Filtra el historial de commits por una palabra clave en el mensaje del commit.
  
git remote show [nombre_repositorio]:
  Muestra información detallada sobre un repositorio remoto.
  
git remote prune origin:
  Elimina las referencias locales a ramas remotas eliminadas.
  
git tag -a [nombre] -m "[mensaje]":
  Crea un nuevo tag anotado con un mensaje.
  
git tag -d [nombre]:
  Elimina un tag local.
  
git push --tags:
  Sube todos los tags locales al repositorio remoto.
  
git checkout -- [archivo]:
  Desecha los cambios no confirmados en un archivo.
  
git revert [commit]:
  Deshace los cambios introducidos por un commit específico, creando un nuevo commit que revierte esos cambios.
  
git reflog:
  Muestra un registro de las referencias de Git que han sido modificadas en el repositorio.
  
git show-ref:
  Muestra las referencias de Git (branches, tags, etc.) junto con sus hashes.
  
git remote rename [nombre_actual] [nuevo_nombre]:
  Cambia el nombre de un repositorio remoto.
  
git rm --cached [archivo]:
  Elimina un archivo del área de preparación pero lo conserva en el sistema de archivos.
  
git shortlog:
  Muestra un resumen de commits, agrupados por autor.
  
git clean -f:
  Elimina de forma forzada los archivos no rastreados del directorio de trabajo.
  
git clean -fd:
  Elimina de forma forzada los archivos no rastreados y los directorios vacíos del directorio de trabajo.
  
git log --oneline:
  Muestra un historial de commits en una sola línea por commit.
  
git log --graph:
  Muestra un gráfico ASCII de la historia de ramificaciones y fusiones.
  
git merge --abort:
  Cancela un proceso de fusión en caso de conflicto.
  
git rebase -i [rama]:
  Inicia un rebase interactivo para reorganizar commits en la rama actual.
  
git remote set-url [nombre_repositorio] [nueva_URL]:
  Cambia la URL de un repositorio remoto.
  
git reset [commit]:
  Deshace los commits posteriores a un commit específico, manteniendo los cambios en el directorio de trabajo.
  
git reset --soft [commit]:
  Deshace los commits posteriores a un commit específico, moviendo los cambios al área de preparación.
  
git reset --mixed [commit]:
  Deshace los commits posteriores a un commit específico, pero conserva los cambios en el directorio de trabajo sin añadirlos al área de preparación.
  
git log --follow [archivo]:
  Muestra el historial de cambios de un archivo, incluso si ha sido renombrado.
  
git rebase --continue:
  Continúa con un proceso de rebase después de resolver conflictos.
  
git rebase --skip:
  Omite un commit en un proceso de rebase después de resolver conflictos.
  
git add -p:
  Añade interactivamente partes de cambios en archivos al área de preparación.
  
git stash pop:
  Aplica el cambio más reciente guardado en la pila de cambios y lo elimina de la pila.
  







  

  -------------------------------------------Comandos de MacOS-------------------------------------------  
ls:
  Lista los archivos y directorios en el directorio actual.

cd:
  Cambia el directorio de trabajo.

pwd:
  Muestra el directorio de trabajo actual.

mkdir:
  Crea un nuevo directorio.

touch:
  Crea un nuevo archivo vacío o actualiza la fecha de acceso y modificación de un archivo existente.

rm:
  Elimina archivos o directorios.

cp:
  Copia archivos y directorios.

mv:
  Mueve o renombra archivos y directorios.

cat:
  Concatena y muestra contenido de archivos.

more:
  Muestra el contenido de un archivo página por página.

less:
  Muestra el contenido de un archivo de manera interactiva, permitiendo desplazarse hacia arriba y hacia abajo.

grep:
  Busca patrones en archivos.

find:
  Busca archivos y directorios.

chmod:
  Cambia los permisos de archivos y directorios.

chown:
  Cambia el propietario y el grupo de archivos y directorios.

ps:
  Muestra los procesos en ejecución.

kill:
  Envía una señal a un proceso para finalizarlo.

top:
  Muestra información sobre los procesos y su uso de recursos en tiempo real.

sudo:
  Ejecuta un comando con privilegios de superusuario.

df:
  Muestra el espacio en disco disponible en los sistemas de archivos.

tar:
  Crea, modifica y extrae archivos comprimidos en formato tar.

gzip:
  Comprime o descomprime archivos utilizando el algoritmo de compresión gzip.

gunzip:
  Descomprime archivos comprimidos con gzip.

zip:
  Crea archivos comprimidos en formato zip.

unzip:
  Extrae archivos de un archivo comprimido en formato zip.

curl:
  Permite transferir datos desde o hacia un servidor utilizando diferentes protocolos.

ssh:
  Conecta a un servidor remoto utilizando el protocolo SSH.

scp:
  Copia archivos de forma segura entre sistemas a través de SSH.

rsync:
  Sincroniza archivos y directorios entre sistemas de forma eficiente.

ping:
  Envía paquetes ICMP a un host para verificar su accesibilidad y tiempo de respuesta.

traceroute:
  Muestra la ruta que siguen los paquetes para llegar a un host específico.

whois:
  Obtiene información sobre un nombre de dominio o una dirección IP desde una base de datos Whois.

hostname:
  Muestra o configura el nombre del sistema.

uptime:
  Muestra el tiempo que ha estado encendido el sistema y carga promedio.

date:
  Muestra o configura la fecha y la hora del sistema.

cal:
  Muestra un calendario del mes actual o de un mes específico.

history:
  Muestra el historial de comandos ejecutados anteriormente en la Terminal.

echo:
  Muestra texto en la salida estándar o redirige texto a un archivo.

wc:
  Cuenta líneas, palabras y caracteres en un archivo.

nano:
  Editor de texto en la Terminal para editar archivos de texto de manera sencilla.